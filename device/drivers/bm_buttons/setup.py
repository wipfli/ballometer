import setuptools

setuptools.setup(
    name='bm_buttons',
    version='0.1',
    description='Python driver to read out the ballometer buttons',
    author='Oliver Wipfli',
    author_email='email@example.com',
    url='https://github.com/wipfli/ballometer',
    packages=setuptools.find_packages(),
    install_requires=[],
    )
