from setuptools import setup, find_packages

setup(name='ballometer',
      version='1.0.3',
      description='The ballometer is a sensor box for hot air balloons',
      url='https://github.com/wipfli/ballometer/',
      author='wipfli',
      python_requires='>=3.6',
      packages=find_packages())
